// This page has been merged into TerritoiresPage (unified map + list view)
// Redirect kept for backwards compatibility
import { Navigate } from "react-router-dom";
const CarteTerritoiresPage = () => <Navigate to="/territoires" replace />;
export default CarteTerritoiresPage;
